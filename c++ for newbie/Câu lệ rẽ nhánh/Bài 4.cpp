#include "iostream"
using namespace std;
int main(){
    string a,b;
    cin>>a>>b;
    if (a==b){
    std::cout<<"two people have the same name";
    }
    else{
    std::cout<<"two people don't have the same name";
    }
}